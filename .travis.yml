language: php

php:
  - 5.4

before_script:
  - echo "docs_path: '/home/travis/build/asgardphp/website/docs'" > config/local.yml
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar install --prefer-source --no-interaction --dev

script: phpunit